import styled from "@emotion/styled";
import { EditText } from 'react-edit-text';

const EditableNameHeader = styled(EditText)`
  font-size: 2.5rem;
  margin-bottom: 1rem;
`;

export default EditableNameHeader;
