import styled from "@emotion/styled";

const Title = styled.h5`
  font-size: 1.4rem;
  font-weight: 700;
  display: block;
  line-height: 1%.4rem;
  margin: 0;
  padding: 16px 4px;
`;

export default Title;
