import styled from "@emotion/styled";

const DeleteButton = styled.span`
  cursor: pointer;
  float: right;
  font-weight: 800;
  margin-right: 10px;

  &:hover {
    color: red;
  }
`;

export default DeleteButton;
