import styled from "@emotion/styled";

const Note = styled.h1`
  color: #5a5656;
  font-size: 1rem;
  font-weight: 500;
  line-height: 1.4rem;
  padding: 16px 4px;
  word-wrap: break-word;
`;

export default Note;
